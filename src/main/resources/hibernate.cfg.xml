<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
  <session-factory>
    <!-- Kết nối đến cơ sở dữ liệu -->
    <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
    <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/tracnghiem</property>
    <property name="hibernate.connection.username">root</property>
    <property name="hibernate.connection.password"></property>

    <!-- Sử dụng HikariCP (tăng tốc kết nối) -->
    <property name="hibernate.hikari.minimumIdle">5</property>
    <property name="hibernate.hikari.maximumPoolSize">20</property>
    <property name="hibernate.hikari.idleTimeout">30000</property>
    <property name="hibernate.hikari.connectionTimeout">30000</property>
    <property name="hibernate.hikari.maxLifetime">1800000</property>

    <!-- Dialect Hibernate -->
    <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

    <!-- Chế độ cập nhật database -->
    <property name="hibernate.hbm2ddl.auto">update</property>

    <!-- Giảm log Hibernate -->
    <property name="hibernate.show_sql">false</property>
    <property name="hibernate.format_sql">false</property>

    <!-- Mapping -->
    <mapping class="DTO.UserDTO"/>
    <mapping class="DTO.TopicDTO"/>
    <mapping class="DTO.TestDTO"/>
    <mapping class="DTO.TestCodeDTO"/>
    <mapping class="DTO.ResultDTO"/>
    <mapping class="DTO.QuestionDTO"/>
    <mapping class="DTO.LogDTO"/>
    <mapping class="DTO.AnswerDTO"/>
  </session-factory>
</hibernate-configuration>
